export enum MarketplaceIndex {
  Bookmark = 'bookmark',
  Asset = 'asset',
  DDOStatus = 'status',
}

export enum Status {
  Accepted = 'ACCEPTED',
  Pending = 'PENDING',
}

export enum SourceType {
  Elasticsearch = 'Elasticsearch',
}
